# Build artifacts (regenerated by build_native.sh / CMake)
build_*/
build/
out/
module_stage/
*.o
*.obj
*.obj.o.d
*.so.debug
*.pdb

# CMake generated files
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
CMakeUserPresets.json

# Node / npm (webui)
webui/node_modules/
webui/dist/
webui/.vite/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Intentionally DO NOT ignore:
# - prebuilt/<abi>/*.so  (committed binaries, per maintainer's choice)
# - prebuilt/<abi>/zygiskd (committed binaries, per maintainer's choice)
# - blobs/machikado.*  / blobs/mazoku (committed data blobs)
# - webui/package-lock.json (committed for reproducible installs)

# IDE / editor cruft
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# OS / misc
.env
.env.local
.env.*.local
*.bak
*.tmp
